import random  # This loads the built-in random module so we can use random number functions

# Repeat this block of code 3 times
for i in range(3):
    # Print a random whole number between 10 and 20 (including both 10 and 20)
    print(random.randint(10, 20))
