# This imports Python's built-in "random" module, which helps us generate random numbers.
import random

# We start a loop that will run 3 times
for i in range(3):
    # Inside the loop, we print a random decimal number between 0.0 and 1.0
    print(random.random())
