# Defines a function named add that returns the sum of two numbers.
def add(a, b):
    return a + b

# Defines a function named subtract that returns the difference between two numbers.


def subtract(a, b):
    return a - b
