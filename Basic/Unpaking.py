cordinates = (1, 2, 3,)
x, y, z = cordinates
print(f"x: {x}, y: {y}, z: {z}")
# This code demonstrates unpacking a tuple into multiple variables.
# The tuple cordinates contains three elements, which are unpacked into x, y, and z.
